<p><%= link_to line_item.title, item_path(line_item.item) %> | <%= number_to_currency(line_item.item_price) %> | Quantity: <%= line_item.quantity %> | <% if line_item.inventory < 5 %>
    <strong>There are only <%= line_item.inventory %> left in stock.</strong> <% end %>
</p>